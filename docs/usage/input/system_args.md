---
hide:
  - toc
---
These system arguments have defaults for every job type. They can be overwritten as described in the [Introduction](index.md). Not all system arguments apply to every job type, e.g. python tasks do not use the `java_*` arguments and locally run tasks to not use the `ecs_*` arguments.


::: runzi.arguments.SystemArgs
    options:
      show_source: false
      inherited_members: true
